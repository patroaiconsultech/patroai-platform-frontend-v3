# AO-01 post-upload gate

Evidence required from GitHub:

```text
repository URL
repository immutable ID
initial source commit
Node 20.20.2
npm 10.8.2
registry URL
package-lock.json
package-lock SHA-256
npm ci log
31+ tests
Vite build log
dist index SHA-256
dist tree SHA-256
workflow run ID and attempt
artifact digest
```

Then execute:

```text
HTTPS smoke
Lighthouse /
Lighthouse /app
Chrome Android installation
Samsung Internet installation
Safari iPhone Add to Home Screen
Safari iPad portrait/landscape/split view
Alpha 2 → Alpha 2.2 controlled update
Cache Storage inspection after authenticated use
logout storage inspection
rollback drill
```

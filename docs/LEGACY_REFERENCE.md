# Legacy reference

Repository used only as a feature and UX reference:

```text
https://github.com/patroaiconsultech/ORKIO_FRONTEND_AO67L_CONSOLIDATED_RC_FULL
branch=main
commit=NOT_PROVEN
```

Publicly observed capabilities:

```text
public/manifest.webmanifest
public/sw.js
public/icons/
public/apple-touch-icon.png
src/routes/Landing.jsx
src/routes/AppConsole.jsx
service-worker registration in src/main.jsx
separate / and /app routes
```

The legacy repository was not used as the byte baseline. The exact byte
baseline for this successor is the locally preserved
`ORKIO_FRONTEND_V2_PREMIUM_ROOT_READY.zip`.

No claim is made that GitHub main, a historical ZIP and production contain the
same bytes.
